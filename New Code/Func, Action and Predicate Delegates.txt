using System;
using System.Collections.Generic;
using System.Text;

namespace Practice_Project
{

    public delegate double delegate1(int x, int y, double z);
    public delegate void delegate2(int x, int y, double z);
    public delegate bool delegate3(string str);


    class Anonymous
    {
        public static double AddNum1(int x,int y,double z)
        {
            return x + y + z;

        }
        public static void AddNum2(int x, int y, double z)
        {
            Console.WriteLine(x + y + z);

        }
        public static bool CheckLenght(string str)
        {
            if(str.Length>5)
            {
                return true;
            }
            return false;
        }
        static void Main(string[] args)
        {

            delegate1 obj1 = AddNum1;
            double result = obj1.Invoke(12, 13, 23.5);
            Console.WriteLine(result);
            delegate2 obj2 = AddNum2;
            obj2.Invoke(10, 20, 30);
            delegate3 obj3 = CheckLenght;
          bool checck=  obj3.Invoke("Sandip Mistry");
            Console.WriteLine(checck);
        }
    }
}
###########
using System;
using System.Collections.Generic;
using System.Text;

namespace Practice_Project
{

    


    class Anonymous
    {
        public static double AddNum1(int x,float y,double z)
        {
            return x + y + z;

        }
        public static void AddNum2(int x, float y, double z)
        {
            Console.WriteLine(x + y + z);

        }
        public static bool CheckLenght(string str)
        {
            if(str.Length>5)
            {
                return true;
            }
            return false;
        }
        static void Main(string[] args)
        {

            Func<int, float, double, double> obj1 = AddNum1;
            double result = obj1.Invoke(12, 13, 23.5);
            Console.WriteLine(result);
            Action<int, float, double> obj2 = AddNum2;
            obj2.Invoke(10, 20, 30);
            Predicate<string> obj3 = CheckLenght;
          bool checck=  obj3.Invoke("Sandip Mistry");
            Console.WriteLine(checck);
        }
    }
}
#########
using System;
using System.Collections.Generic;
using System.Text;

namespace Practice_Project
{

    


    class Anonymous
    {
       
        
        static void Main(string[] args)
        {

            Func<int, float, double, double> obj1 = (x, y, z) =>
            {
                return x + y + z;
            };
            double result = obj1.Invoke(12, 13, 23.5);
            Console.WriteLine(result);
            Action<int, float, double> obj2 = (x, y, z) =>
            {
                Console.WriteLine(x + y + z);
            };
            obj2.Invoke(10, 20, 30);
            Predicate<string> obj3 = (str)=>
            {
                if (str.Length > 5)
                {
                    return true;
                }
                return false;
            };
          bool checck=  obj3.Invoke("Sandip Mistry");
            Console.WriteLine(checck);
        }
    }
}
